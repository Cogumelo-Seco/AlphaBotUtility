module.exports = async([ player ], { functions, DCClient, TWClient, VKClient }) => {
    /*const channel = DCClient.channels.cache.get(player.textChannelId);

    channel.send(`Fim da fila!`);

    player.destroy();*/
}